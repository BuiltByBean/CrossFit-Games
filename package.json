{
  "name": "crossfit-games-history",
  "version": "1.0.0",
  "private": true,
  "description": "Every CrossFit Games event since 2011, tagged by fitness domain, with a cross-era GOAT analysis for Individual Men.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "fetch": "node scripts/fetch-games.mjs",
    "fetch:workouts": "node scripts/fetch-workouts.mjs",
    "tag": "node scripts/tag-domains.mjs",
    "tag:movements": "node scripts/tag-movements.mjs",
    "analyze": "node scripts/analyze.mjs",
    "build:data": "npm run fetch && npm run fetch:workouts && npm run tag && npm run tag:movements && npm run analyze",
    "db:push": "prisma db push",
    "db:seed": "node scripts/seed.mjs",
    "db:setup": "npm run db:push && npm run db:seed"
  },
  "dependencies": {
    "next": "^15.5.22",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "//overrides": "Both are transitives of next that ship versions with advisories Railway's scanner blocks on. sharp is an optional dep and unused here (no next/image); postcss is pinned by next to 8.4.31 but 8.x is API-stable, so forcing the patched line is safe. Drop these once next ships them itself.",
  "overrides": {
    "sharp": "^0.35.3",
    "postcss": "^8.5.25"
  },
  "devDependencies": {
    "@prisma/client": "6.3.1",
    "@types/node": "22.13.1",
    "@types/react": "19.0.8",
    "prisma": "6.3.1",
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
